// Given an array of numbers and return an array of numbers incremented by 2
const incrementBy2 = (n) => n + 2;

const a = [1, 2, 3, 4, 5, 6, 7];

console.log(a.map(incrementBy2));

// Given an array of numbers and return an object for each of the elements
